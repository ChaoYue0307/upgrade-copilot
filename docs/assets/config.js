window.UPGRADE_COPILOT_CONFIG = {
  // Set this to a local backend URL to use server-side scans, for example:
  // backendApiUrl: "http://127.0.0.1:8787"
  backendApiUrl: "",
  enableLlm: false,
  saveBackendReports: true,
  backendTimeoutMs: 7000
};
