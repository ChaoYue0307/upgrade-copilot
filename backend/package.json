{
  "name": "upgrade-copilot-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local backend prototype for Upgrade Copilot repo scans, saved reports, and optional LLM upgrade reports.",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/scanner.js && node --check src/llm.js && node --check src/reports.js && node --check scripts/smoke-scan.js",
    "smoke": "node scripts/smoke-scan.js"
  },
  "engines": {
    "node": ">=20"
  }
}
